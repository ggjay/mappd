---
description: 修复后自动推送 GitHub；Vercel 部署需经用户确认
alwaysApply: true
---

# 部署工作流

## GitHub（自动）

完成代码修复或功能改动后，**无需额外询问**，自动执行：

1. `git add` 相关文件
2. `git commit`（简洁说明 why）
3. `git push origin main`（或当前工作分支）

推送到：`https://github.com/ggjay/mappd.git`

## Vercel（需询问）

- **不要**主动执行 `vercel deploy`、`vercel --prod` 或 Vercel CLI 相关部署命令，除非用户明确同意。
- 推送 GitHub 后，若项目已关联 Vercel，**可能会自动触发部署**。在 push 完成后简短告知用户：「已推送到 GitHub；Vercel 若已关联仓库可能会自动部署，是否需要我协助检查部署状态？」
- 仅当用户明确说「部署到 Vercel」「上线」「触发 Vercel」等时，才执行 Vercel 相关操作或协助确认部署。
