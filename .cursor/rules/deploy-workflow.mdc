---
description: 修复后自动推送 GitHub 并触发 Vercel 部署
alwaysApply: true
---

# 部署工作流

## GitHub + Vercel（均自动）

完成代码修复或功能改动后，**无需询问用户**，自动执行：

1. `git add` 相关文件
2. `git commit`（简洁说明 why）
3. `git push origin main`

仓库：`https://github.com/ggjay/mappd.git`

项目已关联 Vercel，**推送到 `main` 即自动触发生产部署**，无需再单独确认或执行 `vercel deploy`。

## 完成后告知用户

推送成功后简短说明：

- 已推送到 GitHub（commit hash）
- Vercel 正在自动部署，可在 https://vercel.com/gouyi-s-projects/mappd/deployments 查看进度

## 例外情况

- **不要**使用 `vercel login` / `vercel --prod` CLI，除非 GitHub 推送连续失败且用户要求改用 CLI。
- 若 `git push` 因网络失败，自动重试 1–2 次后再告知用户。
- **不要** force push `main`，除非用户明确要求。
